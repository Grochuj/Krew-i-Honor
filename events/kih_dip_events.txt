add_namespace = dip_event

country_event = {
	id = dip_event.1
	title = dip_event.1.t
	desc = dip_event.1.d
	picture = GFX_report_event_generic_read_write

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		ai_chance = {
			base = 35
		}
		name = dip_event.1.a
		if = {
			limit = {
				PRL = {
					has_completed_focus = PRL_uklad_warszawski
				}
			}
        }
        diplomatic_relation = {
            country = DIP
            relation = non_aggression_pact
            active = yes
        }
		
	}
	option = {
		ai_chance = {
			base = 10
		}
		name = dip_event.1.b
		add_opinion_modifier = {
			target = PRL
			modifier = small_decrease
		}
		
	}
}